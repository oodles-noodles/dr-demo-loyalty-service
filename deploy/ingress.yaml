apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dr-demo-loyalty-service
  namespace: commerce
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dr-demo-loyalty-service.oodles-noodles.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dr-demo-loyalty-service
                port:
                  number: 80
